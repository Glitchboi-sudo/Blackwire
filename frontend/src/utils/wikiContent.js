// wikiContent — páginas de la wiki in-app (versión condensada de la wiki de
// GitHub). Contenido estático en Markdown, renderizado offline por
// miniMarkdown.js. Generado; editar las fuentes y regenerar.

export const WIKI_PAGES = [
  {
    "id": "intro",
    "title": "Introducción",
    "md": "# Introducción\n\n**BlackWire** es un proxy interceptor HTTP/HTTPS de código abierto para pruebas de seguridad, análisis de tráfico web y debugging de aplicaciones. Es una alternativa ligera, portable y extensible a Burp Suite u OWASP ZAP.\n\n## Concepto central\n\nBlackWire se sitúa **entre tu navegador (o dispositivo) y el servidor**, actuando como intermediario. Todo el tráfico HTTP/HTTPS pasa a través de él, lo que te permite:\n\n- **Capturar** cada request y response en tiempo real.\n- **Inspeccionar** headers, bodies, cookies, tokens y status codes.\n- **Modificar** el tráfico al vuelo (interceptación) o reenviarlo modificado (Repeater).\n- **Analizar** patrones de datos sensibles, tokens JWT y diferencias entre respuestas.\n\nEl motor de interceptación es **mitmproxy**. Para ver tráfico HTTPS descifrado necesitas confiar en el certificado CA de mitmproxy (ver *Primeros pasos*).\n\n## Qué ofrece\n\n- **Interceptor** con control total sobre requests/responses y varios modos de proxy.\n- **HTTPQL**, lenguaje de consulta para filtrar el historial de tráfico.\n- **Repeater**, **Intruder**, **Collections** y **Session Rules** para testing iterativo y automatizado.\n- **Sensitive Discovery** (50+ patrones de secrets + entropía de Shannon).\n- **JWT analyzer**, **Cipher** (encoder/decoder encadenable) y **WebSocket viewer**.\n- **Compare** con diff visual, **Site Map** jerárquico e **import/export** compatible con Burp Suite.\n- Sistema de **extensiones en Python** con UI auto-generada.\n\n## Organización del trabajo\n\nCada **proyecto** es un workspace aislado con su propia base de datos SQLite (`projects/{nombre}/blackwire.db`). Contiene su historial HTTP, requests de Repeater, collections, session rules, filter presets, configuración de scope y commits de Git.\n\nCrea o cambia de proyecto desde la pestaña **Projects**. Elimínalo con el botón de borrar de su tarjeta (borra todos sus datos de forma permanente).\n"
  },
  {
    "id": "quickstart",
    "title": "Primeros pasos",
    "md": "# Primeros pasos\n\nCaptura tu primera petición en pocos minutos. BlackWire ya está corriendo; solo hay que arrancar el proxy y apuntar tu navegador hacia él.\n\n## 1. Crear un proyecto\n\nEn la pestaña **Projects** crea un proyecto nuevo, ingresa un nombre y confírmalo.\n\n## 2. Iniciar el proxy\n\n1. Click en **▶ Start** (esquina superior derecha).\n2. Confirma el puerto (por defecto **8080**).\n3. El proxy queda escuchando en `localhost:8080`.\n\n## 3. Configurar el navegador\n\nApunta la configuración de proxy de tu navegador (o del sistema) hacia BlackWire:\n\n| Campo | Valor |\n|-------|-------|\n| HTTP Proxy | `localhost` |\n| Puerto | `8080` |\n| HTTPS | Marca \"usar también para HTTPS\" |\n\n- **Firefox**: Preferences → Network Settings → *Manual Proxy Configuration*.\n- **FoxyProxy**: crea un proxy `localhost:8080`, tipo HTTP, y actívalo.\n- **Proxy del sistema (GNOME)**: `gsettings set org.gnome.system.proxy mode 'manual'` (host `localhost`, puerto `8080`).\n\n> Consejo: usa un navegador o perfil dedicado para las pruebas, así no enrutas tu navegación habitual por el proxy.\n\n## 4. Instalar el certificado CA (para HTTPS)\n\nPara ver tráfico HTTPS descifrado, tu navegador o sistema debe confiar en el certificado CA de mitmproxy, ubicado en:\n\n```\n~/.mitmproxy/mitmproxy-ca-cert.pem\n```\n\n**Firefox**: Preferences → Privacy & Security → Certificates → View Certificates → *Authorities* → *Import* → selecciona el `.pem` → marca \"Trust this CA to identify websites\".\n\n**Chrome/Chromium**: Settings → Privacy and Security → Security → Manage Certificates → *Authorities* → *Import* → selecciona el `.pem` → marca \"Trust this certificate for identifying websites\".\n\n**A nivel de sistema (Linux):**\n\n```bash\n# Ubuntu/Debian\nsudo cp ~/.mitmproxy/mitmproxy-ca-cert.pem /usr/local/share/ca-certificates/mitmproxy.crt\nsudo update-ca-certificates\n\n# Fedora/RHEL\nsudo cp ~/.mitmproxy/mitmproxy-ca-cert.pem /etc/pki/ca-trust/source/anchors/\nsudo update-ca-trust\n\n# Arch Linux\nsudo trust anchor ~/.mitmproxy/mitmproxy-ca-cert.pem\n```\n\n> Si la interceptación HTTPS deja de funcionar (certificado corrupto o caducado), regenera el CA borrando `~/.mitmproxy` y volviéndolo a generar.\n\n## 5. Capturar e inspeccionar\n\n1. Navega en tu navegador configurado (ej. `http://example.com`).\n2. Abre la pestaña **History**: verás cada request en tiempo real (método, host, path, status, tamaño, timestamp).\n3. Click en una request → el panel derecho muestra las pestañas **Request** y **Response**. Usa *Pretty* para formatear JSON/XML/HTML.\n\n## 6. Reenviar al Repeater\n\nClick derecho en una request → *Send to Repeater* → pestaña **Repeater** → modifica y pulsa **Send** (`Ctrl+Enter`).\n\n## 7. Interceptar en vivo (opcional)\n\nBotón **Intercept** → activa *Intercept Requests*/*Responses*. Cada request se pausa: modifícala y pulsa **Forward** para enviarla o **Drop** para descartarla.\n"
  },
  {
    "id": "features",
    "title": "Funcionalidades",
    "md": "# Funcionalidades\n\n## Proxy Interceptor\n\nCaptura, inspecciona y modifica tráfico HTTP/HTTPS en tiempo real. Modos: **Regular** (estándar), **Upstream** (cadena de proxies), **SOCKS5**, **Reverse** y **Transparent**.\n\nCon **Intercept** activo, cada request/response se pausa para editar antes de reenviarla (*Forward*), descartarla (*Drop*) o modificar headers/body/método/URL.\n\n**Scope Rules** definen qué tráfico interceptar mediante reglas **Include** (solo URLs que coincidan) y **Exclude** (ignorar coincidencias), con patrones tipo `*.example.com/*` o regex. Los cambios de scope se aplican también al historial ya capturado.\n\n## Repeater\n\nReenvía requests capturados para pruebas iterativas.\n\n- Edita método, URL, headers y body.\n- Historial de navegación (atrás/adelante), auto-guardado y *Pretty Print*.\n- Manejo de redirects: *No Redirect* (manual) o *Auto Follow*.\n- Atajos: `Ctrl+Enter` enviar.\n\n## Intruder\n\nFuzzing automatizado: define posiciones de payload dentro de un request base y ejecuta un conjunto de valores para probar múltiples variantes. Envía un request desde History con click derecho → *Send to Intruder* y revisa los resultados por status y tamaño de respuesta.\n\n## History & Site Map\n\n- **History**: tabla cronológica de todas las requests. El menú contextual (click derecho) ofrece: *Send to Repeater*, *Send to Intruder*, *Add to Collection*, *Compare (A)/(B)*, *Add Host to Scope* y *Delete*.\n- **Site Map**: vista jerárquica en árbol de endpoints, con conteo de requests por nodo y badges de métodos HTTP. Click en un nodo para ver sus requests.\n\n## Collections\n\nAgrupa requests en secuencias ejecutables con extracción y sustitución de variables.\n\n- Añade requests desde History (*Add to Collection*).\n- **Extracción de variables**: source `body` o `header`, y `Path` con sintaxis **JSONPath** (ej. `$.data.token`).\n- **Uso**: referencia variables en requests posteriores con `{{nombre}}`.\n- Ejecuta paso a paso con **▶ Send Next**; el panel *Variables* muestra los valores en tiempo real.\n\nEjemplo: Login extrae `$.token` → `{{auth_token}}`, y el siguiente request lo usa en `Authorization: Bearer {{auth_token}}`.\n\n## Session Rules\n\nAutomatiza la extracción de valores desde responses (o requests) mediante regex. Configura *When* (`response`/`request`), *Target* (`body`/`headers`), el patrón regex con grupo de captura y el nombre de variable. Las variables quedan disponibles en Collections vía `{{var}}`.\n\n## Sensitive Discovery\n\nEscanea el historial en busca de secrets y datos sensibles (100% client-side; nada sale del navegador).\n\n- **Detecta**: AWS keys, private keys (RSA/SSH/PGP), API tokens (GitHub, Stripe, Slack, etc.), database URLs, JWT/OAuth/Bearer tokens, credenciales cloud y PII (emails, tarjetas con validación Luhn, teléfonos).\n- **Shannon Entropy Filter**: reduce falsos positivos midiendo la aleatoriedad de los strings. `2.5–3.0` = más resultados; `3.5` (default) = balance; `4.0–4.5` = alta precisión.\n- **Uso**: pestaña **Sensitive** → *Scan All Requests* → ajusta el slider si hay demasiados falsos positivos.\n\n## JWT Analyzer\n\nPestaña **Cipher** → sub-tab **JWT**. Decodifica header, payload y signature, y documenta ataques comunes (None Algorithm, Key Confusion RS256→HS256, brute-force de secret débil).\n\n## Cipher\n\nEncoder/decoder visual con recetas encadenables. Pega datos, añade operaciones y el output se actualiza automáticamente. Categorías: **Encoding** (Base64, URL, Hex), **Hashing** (MD5, SHA, HMAC), **Compression** (Gzip, Zlib), **Encryption** (AES, DES/3DES, RC4, ChaCha20, XOR), y **String Operations**.\n\n## WebSocket Viewer\n\nPestaña **History** → sub-tab **WS**. Lista conexiones WebSocket, muestra frames en ambas direcciones (↑ enviado, ↓ recibido) con tipo/timestamp/longitud/contenido, y permite reenviar frames editados.\n\n## Compare\n\nDiff visual lado a lado. Marca dos requests con *Compare (A)* y *Compare (B)*, ve a la pestaña **Compare** y alterna entre Request/Response. Usa el algoritmo LCS: verde = añadido, rojo = eliminado.\n\n## Import / Export\n\nPor proyecto: **Exportar** en *Blackwire Format* (JSON completo) o *Burp Suite XML*; **Importar** con *Merge* (combina), *Replace* (sobrescribe) o *Burp XML*.\n"
  },
  {
    "id": "httpql",
    "title": "HTTPQL",
    "md": "# HTTPQL\n\nHTTPQL es un lenguaje de consulta (inspirado en Caido) para filtrar el historial de tráfico. Escribe la query en el campo de búsqueda de **History**.\n\n## Sintaxis\n\n```\ncampo operador valor\n```\n\n## Campos\n\n**Request:**\n\n| Campo | Descripción |\n|-------|-------------|\n| `req.method` | Método HTTP (GET, POST…) |\n| `req.host` | Hostname |\n| `req.path` | Path + query string |\n| `req.port` | Puerto |\n| `req.ext` | Extensión de archivo (`js`, `css`) |\n| `req.query` | Query string completo |\n| `req.raw` | Request completo (headers + body) |\n| `req.len` | Tamaño del request |\n| `req.tls` | Boolean: si usa HTTPS |\n\n**Response:** `resp.code` (status), `resp.raw` (response completo), `resp.len` (tamaño).\n\n## Operadores\n\n**String**: `eq` (igual), `ne` (distinto), `cont` (contiene), `ncont` (no contiene), `like`/`nlike` (case-insensitive), `regex`/`nregex`.\n\n**Numéricos**: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`.\n\n**Lógicos**: `AND`, `OR`, `NOT`, y paréntesis para agrupar.\n\n## Ejemplos\n\n```httpql\nreq.method eq GET\nreq.host cont example.com\nresp.code gte 400\nreq.method eq POST AND resp.code eq 200\nreq.path regex /api/v[0-9]+/users\nNOT req.host cont google.com\n(req.method eq POST OR req.method eq PUT) AND resp.code lt 300\nreq.ext eq js\nresp.len gt 10000\nreq.tls eq true\n```\n\n## Filter Presets\n\nJunto al campo de filtro, el botón desplegable ofrece **presets predefinidos** (errores, solo GET/POST, 2xx/3xx) y te deja **guardar** tus propias queries: escríbela, dale un nombre y quedará disponible en el dropdown.\n"
  },
  {
    "id": "extensions",
    "title": "Extensiones",
    "md": "# Extensiones\n\nLas extensiones son plugins en Python que manipulan el tráfico HTTP, pueden añadir pestañas a la UI y se configuran con formularios. Cada extensión tiene **acceso completo a `mitmproxy.http.HTTPFlow`**: puede leer y reescribir cualquier parte de una request o response.\n\nLas extensiones viven en `backend/extensions/`. Habilítalas y configúralas desde la pestaña **Extensions**.\n\n> **Tras crear o modificar una extensión hay que reiniciar el backend** (`make restart`). El descubrimiento ocurre al arrancar; no hay hot-reload.\n\n## Tipos (en orden de preferencia)\n\n| Tipo | UI | Caso de uso |\n|------|----|-------------|\n| **Schema-Driven** | Auto-generada desde `ui_schema` | Formularios simples. **Preferido.** |\n| **Dynamic JSX** | Archivo `.ui.jsx` junto al `.py` | UIs ricas sin recompilar el frontend. |\n| **Custom React** | Componente hardcoded | Legacy. Evítalo. |\n\n## Ciclo de vida\n\nCada flujo pasa por hooks opcionales (implementa solo los que necesites):\n\n```\non_load()  ->  on_request()  ->  [proxy reenvía]  ->  on_response()\n```\n\nTambién existe `on_websocket_message`. Todos reciben `cfg` (config de esta extensión) y `full_config` (config del proyecto).\n\n## Esqueleto mínimo (Schema-Driven)\n\nLa UI se genera sola a partir de `ui_schema`:\n\n```python\n# Custom Header Injector - inyecta un header en cada request\n\nEXTENSION_META = {\n    \"name\": \"custom_header\",\n    \"title\": \"Custom Header\",\n    \"description\": \"Inyecta un header personalizado\",\n    \"ui_schema\": {\n        \"type\": \"schema-driven\",\n        \"fields\": [\n            {\"name\": \"header_name\", \"label\": \"Header Name\",\n             \"type\": \"text\", \"default\": \"X-Custom-Header\"},\n            {\"name\": \"header_value\", \"label\": \"Header Value\",\n             \"type\": \"text\", \"default\": \"\"},\n        ],\n    },\n    \"default_config\": {\n        \"enabled\": False,\n        \"header_name\": \"X-Custom-Header\",\n        \"header_value\": \"\",\n    },\n}\n\nfrom mitmproxy import http\n\n\nclass CustomHeaderExtension:\n    name = \"custom_header\"\n\n    def on_request(self, flow: http.HTTPFlow, cfg: dict, full_config: dict):\n        if not cfg.get(\"enabled\"):\n            return\n        value = cfg.get(\"header_value\")\n        if value:\n            flow.request.headers[cfg[\"header_name\"]] = value\n\n\ndef register():\n    return CustomHeaderExtension()\n```\n\nDespués: `make restart` → pestaña **Extensions** → marca *Enabled* → configura los campos.\n\n## Objeto Flow (referencia rápida)\n\n```python\nflow.request.method / url / pretty_host / port / path\nflow.request.headers        # dict-like\nflow.request.content        # bytes;  flow.request.text -> str\nflow.response.status_code / headers / content / text\n\n# Modificar\nflow.request.url = \"https://newdomain.com/path\"\nflow.request.headers[\"X-Custom\"] = \"value\"\nflow.request.content = b'{\"modified\": true}'\n```\n\n## Reglas importantes\n\n- El nombre debe coincidir en `EXTENSION_META[\"name\"]`, el atributo `name` de la clase y (para Dynamic JSX) la clave de `window.BlackwireExtensions`.\n- Si tu extensión importa una librería de terceros, **añádela a `requirements.txt`** o fallará silenciosamente en otros entornos.\n"
  },
  {
    "id": "troubleshooting",
    "title": "Solución de problemas",
    "md": "# Solución de problemas\n\n## HTTPS no se descifra / certificado no funciona\n\n1. Reinstala el certificado CA: borra `~/.mitmproxy` y regenéralo, luego reinstálalo en el navegador/sistema (ver *Primeros pasos*).\n2. Verifica que está instalado:\n   - Firefox: Preferences → Certificates → View Certificates → *Authorities* → busca \"mitmproxy\".\n   - Chrome: `certutil -d sql:$HOME/.pki/nssdb -L | grep mitmproxy`.\n3. Revisa la config de proxy del navegador: debe ser `localhost:8080` y con \"usar para HTTPS\" marcado.\n\n## El proxy no captura tráfico\n\n- **Proxy corriendo**: el botón **▶ Start** debe estar en verde.\n- **Config del navegador**: HTTP y HTTPS apuntando a `localhost:8080`.\n- **Certificado**: instalado (ver arriba) si es tráfico HTTPS.\n- **Scope rules**: pestaña **Scope** → revisa las reglas include/exclude; quítalas temporalmente para descartar que estén filtrando el tráfico.\n\n## Puerto en uso (`Address already in use`)\n\n```bash\nlsof -i :5000        # o :8080 — identifica el PID\nkill <PID>\n```\n\n## La interfaz no carga (página en blanco)\n\n1. Backend vivo: `curl http://localhost:5000/api/projects`.\n2. Consola del navegador (F12 → Console) para ver errores.\n3. Recarga forzada: `Ctrl+Shift+R`.\n4. Si el frontend está desactualizado, recompílalo (`make compile`). La carga lenta suele deberse a que falta **Node.js**, necesario para transpilar el JSX con Sucrase.\n\n## Una extensión no aparece\n\n- El archivo `.py` existe en `backend/extensions/`.\n- `EXTENSION_META` tiene al menos `name`, `title`, `description` y `default_config`.\n- Existe la función `register()` que retorna la instancia de la clase.\n- **Reinicia el backend** (`make restart`) y revisa la salida en busca de errores.\n\n## Base de datos bloqueada o corrupta\n\n1. Detén y reinicia BlackWire, esperando unos segundos a que se cierren las conexiones.\n2. Verifica integridad: `sqlite3 projects/mi-proyecto/blackwire.db \"PRAGMA integrity_check;\"`.\n3. Respalda antes de recuperar: copia el `.db` y usa `.dump` para reconstruirlo.\n\n## No puedo detener el servidor\n\n1. Botón ⏻ del header, o endpoint de apagado: `curl -X POST http://localhost:5000/api/shutdown`.\n2. Localiza y mata el proceso: `ps aux | grep main.py` → `kill <PID>`.\n3. Forzar: `pkill -9 -f \"python.*main.py\"` y `pkill -9 -f mitmproxy`.\n\n---\n\n**Recursos:** código fuente e issues en [github.com/Glitchboi-sudo/Blackwire](https://github.com/Glitchboi-sudo/Blackwire).\n"
  }
];
